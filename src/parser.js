const attributeRegex = /m/g;

const parser = {
  /**
   * Parser.parse
   * accepts a markdown string
   * returns an array of steps
   * 
   * step = {
   *   attributes: {x: 10, y: 100},
   *   content: '<h1>Slide title</h1>'
   * };
   * 
   * @param {string} markdown
   */
  parse: markdown => {}
};

module.exports = parser;
